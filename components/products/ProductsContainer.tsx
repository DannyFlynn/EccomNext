

type ProductsContainer = {
  children: React.ReactNode
}

const ProductsContainer = ({children}: ProductsContainer) => {
  return (
    <>
      {children}
    </>
  )
}

export default ProductsContainer
